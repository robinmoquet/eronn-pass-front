.login-page
    .login
        .login__bottom-action
            text-align right
            margin-top calc(var(--space) * 3)

        .login__new-user
            text-align center
            margin-top calc(var(--space) * 10)
            .button
                margin-left var(--space)
                margin-top var(--space)
            +breakpoint(sm, down)
                margin-top calc(var(--space) * 8)
